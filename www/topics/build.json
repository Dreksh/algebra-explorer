{
    "name": "Build",
    "functions":{},
    "constants": {},
    "actions": [
        {
            "title": "Replace",
            "description": "Operates on an expression to generate a new one. It does not guarantee equality when used in an equation.",
            "parameters": {
                "\\f(x)": "The function to apply to any node",
                "example": "x"
            },
            "matches": [
                {"from": "a", "to": ["\\f(a)"]}
            ]
        }
    ]
}
