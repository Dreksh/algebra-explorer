{
    "name": "Build",
    "functions":{},
    "constants": {},
    "actions": [
        {
            "title": "Replace",
            "description": "Modifying the values in the same way on both sides retain the equality",
            "parameters": {
                "\\f(x)": "The function to apply to any node"
            },
            "matches": [
                {"from": "a", "to": ["\\f(a)"]}
            ]
        }
    ]
}
