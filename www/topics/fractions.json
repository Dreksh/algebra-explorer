{
    "name": "Fractions",
    "functions":{
        "frac": {"arguments": 2, "associative": false, "commutative": false, "latex": "\\frac{\\arg{1}}{\\arg{2}}"},
        "mixedfrac": {"arguments": 3, "latex": "\\arg{1}\\frac{\\arg{2}}{\\arg{3}}"}
    },
    "constants": {},
    "actions": [
        {
            "title": "Expand",
            "description": "Split a mixed number into a whole number and a proper fraction",
            "matches": [
                {"from": "\\mixedfrac(a,b,c)", "to": ["a+\\frac(b,c)"]},
                {"from": "\\frac(\\frac(a,b)*c,d)", "to": ["\\frac(a*c,b*d)"]},
                {"from": "\\frac(a/b,c)", "to": ["\\frac(a,b*c)"]},
                {"from": "\\frac(a,\\frac(b,c)*d)", "to": ["\\frac(a*c,b*d)"]},
                {"from": "\\frac(a,b/c)", "to": ["\\frac(a*c,b)"]}
            ]
        },
        {
            "title": "Change denominator",
            "description": "When you split the cake into smaller chunks, you also get more chunks",
            "parameters": {
                "\\multiplier": "Number of times to split it. It will be multiplied on both the top and bottom of the fraction"
            },
            "matches": [{"from": "\\frac(a,b)", "to": ["\\frac(a*\\multiplier,b*\\multiplier)"]}]
        },
        {
            "title": "To Fraction",
            "description": "Change a number into a fraction",
            "matches": [{"from": "a", "to": ["\\frac(a,1)"]}]
        },
        {
            "title": "Merge",
            "description": "Multiplication and division can be put into the fraction",
            "matches": [
                {"from": "\\frac(a,b)*\\frac(c,d)*e", "to": ["\\frac(a*c,b*d)*e"]},
                {"from": "\\frac(b,c)/a*d", "to": ["\\frac(b,a*c)*d"]},
                {"from": "a*\\frac(b,c)*d", "to": ["\\frac(a*b,c)*d"]}
            ]
        },
        {
            "title": "Factorise",
            "description": "Combine the fractions that contain the same denominator",
            "matches": [
                {"from": "\\frac(a,b)+\\frac(c,b)", "to": ["\\frac(a+c,b)"]},
                {"from": "\\frac(a,b)-\\frac(c,b)", "to": ["\\frac(a-c,b)"]}
            ]
        },
        {
            "title": "Cancel out",
            "description": "Remove factors from the top and bottomt hat are the same",
            "matches": [
                {"from": "\\frac(a*b,a*c)", "to": ["\\frac(b,c)"]}
            ]
        }
    ]
}
